% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_reference_files.R
\name{build_package_reference_docs}
\alias{build_package_reference_docs}
\title{Build all reference MDX files for a package}
\usage{
build_package_reference_docs(
  output_dir,
  pkg = ".",
  config_file = "_starlightr.toml",
  examples = TRUE,
  include_internal = NULL,
  verbose = FALSE
)
}
\arguments{
\item{output_dir}{Path to directory where reference MDX files are saved.}

\item{pkg}{Path to the package directory (default `"."`).}

\item{config_file}{Path to `_starlightr.toml` (relative to `pkg`).}

\item{examples}{Logical, whether to capture and embed example outputs
(default `TRUE`).}

\item{include_internal}{Logical, whether to include internal functions
(those with `@keywords internal`). If `NULL` (default), reads from
`reference.include_internal` in `_starlightr.toml`, falling back to
`FALSE`.}

\item{verbose}{Logical, whether to print debug messages during example
capture (default `FALSE`).}
}
\value{
Invisibly returns a character vector of written file paths.
}
\description{
Convenience wrapper around [build_reference_files()] that processes all
`.Rd` files in the package's `man/` directory.
}
\examples{
\dontrun{
build_package_reference_docs(
  output_dir = "../my-site/src/content/docs/reference"
)

# Include internal functions (overrides config)
build_package_reference_docs(
  output_dir = "../my-site/src/content/docs/reference",
  include_internal = TRUE
)
}
}
