% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_plots.R
\name{plot_conc_time_mean}
\alias{plot_conc_time_mean}
\title{Generate Mean ± SD Concentration-Time Summary Plot}
\usage{
plot_conc_time_mean(
  data,
  x_var,
  y_var,
  id_var,
  color_var,
  split_by = NULL,
  facet_by = NULL,
  shape_var = NULL,
  color_limit = 8,
  stat_type = c("arithmetic", "geometric"),
  errorbar = c("upper", "both"),
  x_units = NULL,
  y_units = NULL,
  x_lab = NULL,
  y_lab = NULL,
  color_lab = NULL,
  shape_lab = NULL,
  style = pkdemo_style(),
  blq = c("drop", "zero", "half_lloq"),
  lloq = NULL,
  mean_below_lloq = c("none", "zero", "missing")
)
}
\arguments{
\item{data}{A data frame containing individual PK data}

\item{x_var}{Time variable (column name, quoted or unquoted, e.g. NTIME or
"NTIME")}

\item{y_var}{Concentration variable (column name, quoted or unquoted, e.g. AVAL)}

\item{id_var}{Subject ID variable (column name, quoted or unquoted, e.g. USUBJID)}

\item{color_var}{Grouping variable mapped to color (column name, quoted or
unquoted, e.g. COHORT or "DOSE")}

\item{split_by}{Character vector of column names used to split the data
into separate plots. NULL (default) returns a single ggplot. When
provided, returns a named list of ggplots, one per unique combination.}

\item{facet_by}{Optional single column name; when set, the returned plot(s)
are faceted by this column via \code{ggplot2::facet_wrap()}. Stratifies
the summary by (color_var, facet_by, x_var) so each panel shows only its
own data. Independent of \code{split_by} (which produces separate plot
objects); the two can be combined.}

\item{shape_var}{Optional single column name mapped to point shape. Useful
for overlaying a secondary stratifier (e.g. visit or period) on top of
the color-coded group. Stratifies the summary by that column.}

\item{color_limit}{Maximum number of \code{color_var} groups that colour
alone encodes. When the number of groups exceeds this, groups are split into
\code{ceiling(n / color_limit)} equal sets --- balanced rather than filled
to \code{color_limit} each (12 groups at 8 gives 6 + 6, not 8 + 4) ---
with colours repeating across sets while linetype changes per set, so each
group stays distinguishable. At or below the limit, every group gets a
unique colour and a solid line. Default: 8.}

\item{stat_type}{Summary statistic family. One of:
\describe{
\item{"arithmetic"}{Arithmetic mean and SD (default)}
\item{"geometric"}{Geometric mean and geometric SD on the log scale;
non-positive y values are dropped before summarising}
}}

\item{errorbar}{Error bar style. One of:
\describe{
\item{"upper"}{Upper whisker only (default)}
\item{"both"}{Upper and lower whiskers}
}}

\item{x_units}{Optional units string appended to the x-axis label.
May also be a named list/character vector keyed by a value of one of
the \code{split_by} columns (e.g. \code{c(Drug1 = "h", Drug2 = "min")});
each split picks the matching entry.}

\item{y_units}{Optional units string appended to the y-axis label.
Accepts the same per-group lookup form as \code{x_units}.}

\item{x_lab}{Override x-axis label. If NULL, derived from x_var + x_units.
Also accepts a named list/character vector keyed by a split value.}

\item{y_lab}{Override y-axis label. If NULL, derived from y_var + y_units.
Same per-group lookup form as \code{x_lab}.}

\item{color_lab}{Legend title. Default: column name of color_var}

\item{shape_lab}{Legend title for the shape aesthetic. Default: column
name of \code{shape_var}.}

\item{style}{A \code{ggstylekit_style_spec} controlling theme, base geom
looks, log axis (\code{logy}), and plot-level/legend appearance. Defaults
to \code{\link{pkdemo_style}()}; pass \code{pkdemo_style(logy = TRUE)}
for a log y-axis. The plot is finished with
\code{\link[ggstylekit]{style_plot}}. Named \code{colors} pins are keyed by
\code{color_var} value (\code{c(`50` = "steelblue")}), resolved per split,
and do not reach the set-based path above \code{color_limit}, which owns
colour itself. A whole-palette choice does reach it, so
\code{pkdemo_style(colorblind = TRUE)} recolours these plots too.}

\item{blq}{How to handle BLQ values — non-numeric strings in a character
y_var column. NA rows are always dropped. One of "drop" (default),
"zero", or "half_lloq"}

\item{lloq}{Scalar numeric LLOQ, or unquoted column name with per-row LLOQ
values. Required when blq = "half_lloq". When \code{mean_below_lloq} is
set, lloq must instead be a scalar numeric or a named numeric vector keyed
by a \code{split_by} value (e.g. \code{c(Drug1 = 25, Drug2 = 0.5)}) —
column references are not supported for that comparison.}

\item{mean_below_lloq}{How to handle per-group mean values that fall below
the LLOQ. One of:
\describe{
\item{"none"}{Leave means as computed (default)}
\item{"zero"}{Replace mean (and its error-bar bounds) with 0}
\item{"missing"}{Replace mean (and its error-bar bounds) with NA, so
the connecting line breaks at that point}
}
Requires \code{lloq}. When lloq is a named lookup, each split's mean is
compared against the matching entry; splits with no matching name are
left unchanged.}
}
\value{
A ggplot object when \code{split_by} is NULL, otherwise a named list of
ggplot objects with names like "var1=val1_var2=val2".
}
\description{
Computes mean and standard deviation (arithmetic by default, optionally
geometric) of a concentration variable at each time point, stratified by a
grouping variable (e.g. cohort or dose level). Plots mean lines with SD
error bars; legend labels include the number of unique subjects per group
formatted as "\if{html}{\out{<group>}} (N = n)". For geometric statistics the error bars
span [mean / GSD, mean × GSD]. Each group gets a distinct colour; once the
number of groups exceeds \code{color_limit}, colours repeat across balanced
sets and linetype varies per set so groups stay distinguishable. When
\code{split_by} is supplied, returns one plot per unique combination of those
columns (useful for separating by analyte, cycle, etc.).
}
